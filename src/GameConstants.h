#pragma once

namespace game
{

constexpr int MAX_NUM_PLAYERS = 4;

constexpr int START_MONEY = 10;

constexpr float TIME_COINS_GEN = 5.f;

constexpr int COINS_PER_CELL = 1;

constexpr int MAX_CELL_LEVEL = 4;

constexpr int COST_CELL_UPGRADE[MAX_CELL_LEVEL] = { 10, 20, 30, 40 };

} // namespace game
